---
- name: Create user "{{ user }}"
  hosts: all
  bocome: true
  gather_facts: true

  tasks:

    - name: Add the user ""{{ user }}" with uid and primary group
      user:
        name: "{{ user }}"
        comment: John Wick
        uid: 1040
